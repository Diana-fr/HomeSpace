{
  "name": "homespace",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm install && node index.js & cd bot-service && npm install && node bot.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "vite": "^4.4.0"
  }
}